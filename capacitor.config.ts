
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.valentia.weatherapp',
  appName: 'WeatherMate',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
